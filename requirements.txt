# The recovery agent, the simulator, the policy engine and the test suite run
# on the Python 3.9+ standard library alone. Nothing below is needed to
# reproduce any number in the README:
#
#   python3 src/runner.py && python3 src/report.py
#   python3 -m unittest discover -s tests -t tests
#
# anthropic is optional, for the LLM diagnosis path that ships disabled
# (diagnose.use_llm=False). It is imported inside the function, never at
# module level, so its absence cannot break anything.
anthropic>=0.125.0
